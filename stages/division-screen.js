export default class division extends Phaser.Scene{

    constructor() {
		super({key:'division'});
	}

    
      
       

    create() {
        this.white = this.add.image(windowWidth / 2.05, widnowHeight / 2, 'white');
		this.white.setDisplaySize(windowWidth * 1.05, widnowHeight * 1.2);
        
        box = document.createElement("input");
        box.type = "text";
        box.id = "EnteredX";

        
        if (A = true) {
            easy;
        } else if(A = false) {
            hard;
        } else{
            console.log.error('NOOOOOO')
        }



        if(D == answer){

        } else {

        }



    }
}